/**
 this class enums all the possible observation object types
 */
export enum ObservationObjectType {
    environment = "environment",
    harvest = "harvest",
    crop ="crop",
    datetime = "datetime",
    coordinate = "coordinate"
}
